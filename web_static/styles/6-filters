/*
 * === Filters class styles ===
 */

/* Styling for the filters section */
.filters {
  background-color: #fff;
  border: 1px solid #ddd;
  border-radius: 4px;
  display: inline-block;
  height: 70px;
  width: 100%;
}

/* Styling for the search button */
.filters button {
  background: #ff5a5f;
  border: none;
  border-radius: 4px;
  color: #fff;
  float: right;
  font-size: 18px;
  height: 48px;
  margin: 12px 30px;
  width: 20%;
}

/* Hover effect for the search button */
.filters button:hover {
  background-color: rgba(255, 90, 95, .9);
}

/* Styling for locations and amenities sections */
.filters .locations,
.filters .amenities {
  display: inline-block;
  height: 100%;
  position: relative;
  width: 25%;
}

/* Right border for the locations section */
.filters .locations {
  border-right: 1px solid #ddd;
}

/* Hover effect for locations and amenities sections */
.filters .locations:hover,
.filters .amenities:hover {
  background-color: #fafafa;
}

/* Popover menu styling for locations and amenities */
.filters .popover {
  background-color: #fafafa;
  border: 1px solid #ddd;
  border-radius: 4px;
  display: none;
  position: absolute;
  width: 100%;
}

/* Display popover on hover for locations and amenities */
.filters .locations:hover .popover,
.filters .amenities:hover .popover {
  display: block;
}

/* List styling within locations section */
.filters .locations ul {
  padding: 6px 0;
}

/* Margin for list items in locations section */
.filters .locations li {
  margin: 0 auto 6px 3em;
}

/* Margin for list items in amenities section */
.filters .amenities li {
  margin: 12px auto 12px 30px;
}

/*
 * --- Header styles within the filters section ---
 */

/* Box sizing for headers within filters */
.filters h2,
.filters h3,
.filters h4 {
  -webkit-box-sizing: border-box;
     -moz-box-sizing: border-box;
          box-sizing: border-box;
}

/* Styling for h2 headers within filters */
.filters h2 {
  font-size: 16px;
  font-weight: bold;
  margin: 12px auto 0 30px;
}

/* Styling for h3 headers within filters */
.filters h3 {
  font-size: 1.17em;
  font-weight: 600;
  height: 50%;
  padding: 15.5px 0 0 30px;
}

/* Styling for h4 headers within filters */
.filters h4 {
  font-size: 14px;
  font-weight: 400;
  height: 50%;
  padding: 4.5px 0 12px 30px;
}

