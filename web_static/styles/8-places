/*
 * === Styles for places class ===
 */

/* Flexbox layout for places section */
.places {
  display: flex;
  flex-flow: row wrap;
  justify-content: center;
  width: 100%;
}

/* Styling for individual articles within places */
.places article {
  border: 1px solid #ff5a5f;
  border-radius: 4px;
  margin: 20px;
  padding: 20px;
  width: 390px;
}

/* Layout for name and price header */
.places .name_and_price {
  align-items: center;
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  margin-bottom: 20px;
}

/* Styling for price per night circle */
.places .price_by_night {
  align-items: center;
  border: 4px solid #ff5a5f;
  border-radius: 50%;
  color: #ff5a5f;
  display: flex;
  font-size: 30px;
  height: 60px;
  justify-content: center;
  min-width: 60px;
}

/* Layout for guests, rooms, and bathrooms info */
.places .information {
  align-items: center;
  border-top: 1px solid #ddd;
  border-bottom: 1px solid #ddd;
  display: flex;
  flex-direction: row;
  height: 80px;
  justify-content: space-around;
  text-align: center;
}

/* Width of each sticker for guests, rooms, and bathrooms */
.places .max_guest,
.places .number_rooms,
.places .number_bathrooms {
  width: 100px;
}

/* Margin around owner info */
.places .user {
  margin: 20px auto;
}

/*
 * --- Header styles within places section ---
 */

/* Styling for h1 headers within places */
.places h1 {
  font-size: 30px;
  font-weight: bold;
  margin: .67em 0;
  width: 100%;
}

/* Styling for h2 headers within places */
.places h2 {
  font-size: 30px;
  font-weight: bold;
  margin: 0 auto;
  text-align: center;
  -webkit-box-sizing: border-box;
     -moz-box-sizing: border-box;
          box-sizing: border-box;
}

footer {
  position: relative;
}
